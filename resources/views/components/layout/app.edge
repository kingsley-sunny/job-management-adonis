<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>
      {{ title || "Your default title" }}
    </title>
    @vite(['resources/css/app.css', 'resources/js/app.js'])
    @if ($slots?.meta)
      {{{ await $slots?.meta() }}}
    @endif
  </head>
  <body>
    @include('components/partials/header/home-header')

    {{{ await $slots?.main() }}}
  </body>
</html>
